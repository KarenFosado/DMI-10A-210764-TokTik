import 'package:flutter/material.dart';

class DiscoverScreeen extends StatelessWidget {
  const DiscoverScreeen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('DiscoverScreen'),
      ),
    );
  }
}
